[build]
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm run start:prod-simple"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3